ARG GO_VERSION
FROM docker.elastic.co/beats-dev/golang-crossbuild:${GO_VERSION}-buster

RUN \
  apt-get update \
  && apt-get install -y zip \
  && mkdir /.cache \
  && chmod 777 /.cache

WORKDIR /go/src/github.com/elastic/fleet-server

ENTRYPOINT [ "make", "release" ]
