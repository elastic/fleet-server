output:
  elasticsearch:
    hosts: ["localhost:9200"]
    username: "elastic"
    password: "changeme"
inputs:
  - type: fleet-server
    server:
      host: "localhost"
      port: 8888
      timeouts:
        read: 20s
        write: 5s
